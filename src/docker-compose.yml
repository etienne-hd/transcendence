volumes:
  database:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: ./data/database
  prometheus_data:
  grafana_data:

services:
  database:
    container_name: database
    networks:
      - unicord
    image: mariadb:12
    env_file: .env
    volumes:
      - database:/var/lib/mysql
    restart: unless-stopped
    expose:
      - "3306"

  nginx:
    container_name: nginx
    networks:
      - unicord
    depends_on:
      - frontend
      - backend
      - grafana
    build:
      context: ./nginx
      args:
        FRONTEND_DOMAIN: ${FRONTEND_DOMAIN}
        BACKEND_DOMAIN: ${BACKEND_DOMAIN}
    env_file: .env
    restart: unless-stopped
    ports:
      - "443:443"

  backend:
    container_name: backend
    networks:
      - unicord
    volumes:
      - ./data/uploads:/app/uploads
    depends_on:
      - database
    build: ./backend
    env_file: .env
    restart: unless-stopped
    expose:
      - "443"

  frontend:
    container_name: frontend
    networks:
      - unicord
    depends_on:
      - backend
    build: ./frontend
    env_file: .env
    restart: unless-stopped
    expose:
      - "3001"

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    networks:
      - unicord
    volumes:
      - ./prometheus:/etc/prometheus
      - prometheus_data:/prometheus
    expose:
      - "9000"

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    networks:
      - unicord
    expose:
      - "3000"
    env_file: .env
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning/datasources:/etc/grafana/provisioning/datasources
      - ./grafana/provisioning/dashboards/dashboard_config.yml:/etc/grafana/provisioning/dashboards/all.yml
      - ./grafana/dashboards:/etc/grafana/provisioning/dashboards/json
    depends_on:
      - prometheus

networks:
  unicord:
    name: unicord
    driver: bridge
