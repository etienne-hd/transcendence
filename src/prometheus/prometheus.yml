global:
  scrape_interval: 15s

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: 'nest-backend'
    scheme: http
    tls_config:
      insecure_skip_verify: true
    static_configs:
      - targets: ['backend:3000']