groups:
  - name: backend_alerts
    rules:
      - alert: BackendHighMemory
        expr: process_resident_memory_bytes > 150000000 #150Mo
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Consommation mémoire critique sur le backend"
